# Personal Website

A simple static website served by **NGINX**, containerized and hosted on **Azure Container Apps** for free.  
The domain is managed through **Cloudflare**, and SSL certificates are automatically handled by **Azure Container Apps**.
